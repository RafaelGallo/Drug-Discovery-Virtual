# Environment the artefacts in this folder were built with.
# scikit-learn is pinned exactly: unpickling a model across a different version emits
# InconsistentVersionWarning and may change behaviour silently.
# Use Python 3.12 or newer; the saved models were generated with Python 3.12.4.
scikit-learn==1.5.0
pandas==2.2.3
numpy==1.26.4
joblib==1.4.2
xgboost==3.0.1      # only needed for all_models/*xgboost*
lightgbm==4.6.0     # only needed for all_models/*lightgbm*
